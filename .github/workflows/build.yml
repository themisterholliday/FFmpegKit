name: Build and Test

on:
  push:
    branches:
      - '*'
  pull_request:
    branches:
      - '*'

jobs:
  buildJob:
    name: Build
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v2
        with:
          submodules: true

      - name: Install Python 3 with Development Headers
        run: |
          brew update
          brew install python@3.11  # Install Python 3.11 using Homebrew
          brew link --overwrite python@3.11
          python3 -m pip install --upgrade pip setuptools wheel  # Upgrade pip and install essential tools

      - name: Verify Python Environment
        run: |
          python3 --version
          python3 -m pip list
          python3 -m pip show setuptools

      - name: Set up Xcode
        run: |
          sudo xcode-select -s /Applications/Xcode_15.2.app/Contents/Developer
          xcode-select -p
          xcodebuild -version

      - name: Build FFmpeg with Swift Package
        run: |
          cd ${{ github.workspace }}  # Ensure we are in the GitHub Actions workspace
          swift package --disable-sandbox BuildFFmpeg enable-libsmbclient notRecompile
